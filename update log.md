# 更新日志（Update Log）

> 本文件记录「DeepSeek-Pet / dsh-pet 整合版」的版本更新历史：更新了什么功能、修复了什么 Bug、发布了什么产物。
> 格式约定：新条目追加在文件顶部对应日期的分节内（最新在上）。

## 版本说明

- **当前版本**：v0.2.0（延续上游 dsh-pet 版本号）
- **产物命名规则**：`DeepSeekPet-<版本>-<平台>-<架构>.<扩展名>`
  - 平台：`mac`（macOS dmg 安装包）/ `win`（Windows 便携版 exe）
  - 架构：`arm64`（Apple 芯片 / 骁龙 Windows）/ `x64`（Intel/AMD）
- **产物存放**：`pack_output/MAC/`（dmg）、`pack_output/WIN/`（exe）

---

## 2026-08-27 —— v0.2.0 独立桌面版（Electron）

### ✨ 新增功能

- **独立桌面版架构**：桌宠脱离 DeepSeek Harness 网页，作为 macOS / Windows 独立应用运行
  - 透明、无边框、置顶窗口 + 点击穿透（点击空白处直达下方应用）
  - 内置本地服务，宠物核心代码零改动复用（同一份源码双端构建：DSH 插件版 + 独立版）
  - 设置/账本/API key 与插件版共享同一份用户数据，切换版本不丢配置
- **macOS dmg 安装包**（arm64）+ **Windows 便携版 exe**（x64 / arm64）
- **应用图标**：macOS 与 Windows 均使用自定义图标 `icon.png`
- **汉堡菜单新增「行为」行**（独立版专属，插件版自动隐藏）：
  - 「程序坞显示」：切换 Dock 中应用图标的显示与隐藏
  - 「前台显示」：强制宠物盖过全屏应用 / 恢复普通置顶
- **远程桌面适配**：通过向日葵等远程控制连接时窗口自动绑定到用户可见的显示器，显示器增删/变化时自动重定位
- **打包产物分目录**：dmg 进 `pack_output/MAC/`，exe 进 `pack_output/WIN/`

### 🐛 修复

- 修复：激活「前台显示」时 macOS 自动隐藏 Dock 图标（层级切换后重新断言 Dock 状态）
- 修复：Windows 便携版音效目录不可见——音效文件夹改为放在 **exe 同级 `sound/` 文件夹**（一眼可见，直接拖入成对文件即自定义音效；exe 目录不可写时自动回落用户数据目录；macOS 版保持用户数据目录 + 「···」按钮一键打开）
- 明确音效命名规则：**只看文件名最后一位**（`1`=点击声、`2`=松手声，前面部分即显示名），例：`小熊猫11.mp3`+`小熊猫12.mp3` → 菜单显示「小熊猫1」，方便同种音效做多种变体
- 修复：独立版动画播放扩展名占位符未注入时自动回落到 .webm
- 修复：音效文件命名（`音效11/12.mp3` → `音效1/2.mp3`，显示名正确为「音效」）

### 📚 文档

- 新增 `update log.md` 更新日志（本文件），README 顶部加入入口链接

### 📦 发布产物

| 文件 | 说明 |
| --- | --- |
| `DeepSeekPet-0.2.0-mac-arm64.dmg` | macOS（Apple 芯片）安装包，本地自签证书签名 |
| `DeepSeekPet-0.2.0-win-x64.exe` | Windows x64 便携版（Intel/AMD，主流机型） |
| `DeepSeekPet-0.2.0-win-arm64.exe` | Windows ARM64 便携版（骁龙机型） |

### ⚠️ 已知说明

- macOS 签名现状：使用本机自签证书（`Deepseek Local`）。本机双击即用；其他 Mac 首次打开需**右键 → 打开**。公开分发前的正式方案：Apple Developer 签名 + 公证（$99/年）。
- Windows 下载文件会有 SmartScreen 提示（未签名），点「更多信息 → 仍要运行」即可。

---

## 2026-08-26 —— 插件整合与功能完善（DSH 插件版）

### ✨ 新增功能

- **插件合并**：将插件 A（小鲸鱼余额挂件）的设置菜单、点击显示余额、记账模式、点击音效移植到插件 B（dsh-pet）
- **汉堡菜单**：悬停宠物显示菜单按钮（大小 / 音效 / 音量 / 间隔 / 用量 / 行为）
- **点击显示余额**：气泡展示余额与「今日已用」，支持 6 档余额事件动画
- **用量双模式**：「本地记账（推荐）」与「API 实时」（余额官方接口查询；今日已用优先平台实时用量并换算，无平台令牌自动回落本地记账）
- **API key 管理弹窗**：保存/清空/更新 API key（本地文件 0600 权限，接口永不回传 key 值）
- **音效系统升级**：
  - 每次打开菜单实时扫描音效目录，新增成对文件自动出现
  - 新命名规则：`<名称>1.mp3`（按压）+ `<名称>2.mp3`（松手），名称即下拉显示名
  - 「···」按钮一键在系统文件管理器打开音效目录（插件自身相对路径定位）
- **动画间隔滑块**：0–90 秒线性调节，持久化保存
- **贴边优化**：全动画统一边距 + 10px 安全垫——宠物可贴近屏幕左右边缘，动画切换零跳跃（基于 97 个动画的逐帧透明像素分析）
- **Safari 兼容**：Safari 下自动切换 .mov（HEVC-Alpha）格式，修复透明通道黑底
- **特别鸣谢弹窗**：三位 B 站 UP 主超链接 + nanonocturne-altago 的 GitHub 项目主页链接 + 页脚
- **悬停提示**：角色命名为「DeepSeek娘」
- **全源码中文注释工程**：18 个核心文件 +1078 行中文注释（AST 级验证零行为变更），最大化可维护性
- **README 重建**：代码来源署名 / 与原版功能对比 / 双下载方式 / 音效命名规则 / 许可说明 / 支持原作者 / 免责声明
- **原 UP 主资源档案**：原包移除内容完整导出至 `原UP主的资源文件/`（按作者分文件夹、保持原目录结构）
- **GitHub 公开仓库**：https://github.com/nanonocturne-altago/DeepSeek-Pet （首次上传 367 个文件）

### 🐛 修复

- 修复：Safari 播放动画黑块（VP9-alpha 透明通道兼容）
- 修复：菜单在屏幕顶部时溢出（空间不足自动翻转）
- 修复：菜单/弹窗被宠物层拦截（z-index 提升到最高）
- 修复：鸣谢弹窗空行高度不足
- 修复：React jsx 工厂 null props 崩溃（span 子元素必须传空对象）
- 修复：钱袋满溢动画贴边时位置跳跃（改为全动画统一边界）
- 修复：音效列表写死（改为目录动态扫描）

### 📦 发布产物

- 本阶段以 DSH 插件形式发布（源码，无独立安装包）

---

## 版本历史对照

| 版本 | 日期 | 里程碑 |
| --- | --- | --- |
| 上游原版 | — | @宇宙之外的浩瀚宇 的 dsh-pet 开源包（MIT） |
| v0.2.0 插件整合版 | 2026-08-26 | 插件 A/B 合并 + 十余项功能完善 |
| v0.2.0 独立桌面版 | 2026-08-27 | Electron 独立应用 + macOS dmg + Windows exe |
